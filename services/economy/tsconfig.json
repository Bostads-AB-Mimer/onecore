{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDirs": ["./src", "./test"],
    "outDir": "./build",
    "types": ["koa-body", "jest"],
    // TODO: Make sure this app builds and runs correctly with paths added
    "paths": {
      "@test/*": ["./test/*"],
      "@src/*": ["./src/*"]
    }
  }

}
